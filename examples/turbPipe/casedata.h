      COMMON /SCNRS/ sc_nrs(10)
      real                sc_nrs
      real ZLENGTH
      integer NSLABS
      integer NELSLAB
      ZLENGTH = sc_nrs(1)
      NSLABS = int(sc_nrs(2))
      NELSLAB = int(sc_nrs(3))