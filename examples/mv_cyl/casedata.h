      COMMON /SCNRS/ sc_nrs(10)
      real                sc_nrs
      real XLEN
      real YLEN
      real ZLEN

      real P_GAMMA
      real P_DELTA
      real P_OMEGA
      real P_AMP

      XLEN = sc_nrs(1)
      YLEN = sc_nrs(2)
      ZLEN = sc_nrs(3)

      P_GAMMA = sc_nrs(4)
      P_DELTA = sc_nrs(5)
      P_OMEGA = sc_nrs(6)
      P_AMP = sc_nrs(7)